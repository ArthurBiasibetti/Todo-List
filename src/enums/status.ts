export enum changeFilterStatus {
  todos = 'concluidos',
  concluidos = 'pendentes',
  pendentes = 'todos',
}

export enum FilterStatus {
  todos = 'todos',
  concluidos = 'concluidos',
  pendentes = 'pendentes',
}
